[project]
name = "youpick-frontend"
version = "0.1.0"
description = "Streamlit UI for YouPick"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27,<1.0",
    "python-dotenv>=1.0,<2.0",
    "streamlit>=1.40,<2.0",
    "authlib>=1.3.2,<2.0",
]

[tool.setuptools]
packages = ["components", "services"]
